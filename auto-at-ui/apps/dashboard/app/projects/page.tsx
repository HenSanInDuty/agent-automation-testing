"use client";
import { FormEvent, useEffect, useState } from "react";
import { useRouter } from "next/navigation";
import { PageHeader } from "../components/page-header";
import { ControlPlaneError } from "../api-client";
import { createProject, createRun, projects, renameTestCase, testCases, type Project, type TestCase } from "../run-api";

export default function ProjectsPage() {
  const apiUrl = process.env.NEXT_PUBLIC_CONTROL_PLANE_URL ?? "http://localhost:7000"; const router = useRouter();
  const [items, setItems] = useState<Project[]>([]); const [tests, setTests] = useState<TestCase[]>([]); const [projectId, setProjectId] = useState(""); const [testId, setTestId] = useState(""); const [url, setUrl] = useState(""); const [projectName, setProjectName] = useState(""); const [testName, setTestName] = useState(""); const [notice, setNotice] = useState("");
  useEffect(() => { projects(apiUrl).then((next) => { setItems(next); if (next[0]) setProjectId(next[0].id); }).catch((error) => setNotice(error instanceof ControlPlaneError ? error.message : "Unable to load projects.")); }, [apiUrl]);
  useEffect(() => { if (!projectId) return; testCases(apiUrl, projectId).then((next) => { setTests(next); setTestId(next[0]?.id ?? ""); }).catch((error) => setNotice(error.message)); }, [apiUrl, projectId]);
  const selected = tests.find((item) => item.id === testId);
  useEffect(() => { setTestName(selected?.name ?? ""); }, [selected]);
  async function addProject(event: FormEvent<HTMLFormElement>) { event.preventDefault(); setNotice(""); try { const project = await createProject(apiUrl, { name: projectName, default_target: "web_ui" }); setItems((current) => [...current, project]); setProjectId(project.id); setProjectName(""); } catch (error) { setNotice(error instanceof ControlPlaneError ? error.message : "Unable to create project."); } }
  async function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); if (!selected) return; try { const created = await createRun(apiUrl, { project_id: projectId, test_case_id: selected.id, target_type: selected.target_type, target_url: url || undefined, runner_config: {}, artifact_policy: { trace_on_failure: true, video_on_failure: true, screenshot_on_failure: true, trace_on_success: true, video_on_success: true, screenshot_on_success: true, retain_days: 30 } }); router.push(`/runs/${created.id}`); } catch (error) { setNotice(error instanceof ControlPlaneError ? error.message : "Unable to create run."); } }
  async function renameSelected() { if (!selected || !testName.trim()) return; try { const renamed = await renameTestCase(apiUrl, projectId, selected.id, testName.trim()); setTests((current) => current.map((item) => item.id === renamed.id ? renamed : item)); setNotice(""); } catch (error) { setNotice(error instanceof ControlPlaneError ? error.message : "Unable to rename test case."); } }
  return <><PageHeader eyebrow="Catalog" title="Projects & Tests" description="Create a project by name, then choose an immutable test revision and start one deterministic run." />{notice && <p className="notice notice--error" role="alert">{notice}</p>}<section className="workspace-section"><h2>Create project</h2><p>The control plane assigns the project UUID automatically.</p><form className="project-create-form" onSubmit={addProject}><label className="field">Project name<input required value={projectName} onChange={(e) => setProjectName(e.target.value)} placeholder="Project-01" /></label><button className="button" type="submit">Create project</button></form></section>{!items.length ? <p className="panel">No projects are available yet. Create one above.</p> : <form className="workspace-section form-grid" onSubmit={submit}><label className="field">Project<select value={projectId} onChange={(e) => setProjectId(e.target.value)}>{items.map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</select></label><label className="field">Test case<select required value={testId} onChange={(e) => setTestId(e.target.value)}>{tests.map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</select></label><label className="field">Target URL <input type="url" value={url} onChange={(e) => setUrl(e.target.value)} placeholder="https://example.test" /></label><div className="field">Immutable revision <code>{selected?.revision ?? "Select a test case"}</code></div><div className="field form-grid--full"><label className="field">Test case name<input value={testName} onChange={(e) => setTestName(e.target.value)} disabled={!selected} /></label><p><small>Only project and tenant administrators can save a new name. The technical ID and revision stay immutable.</small></p><button className="button button--secondary" type="button" disabled={!selected || !testName.trim() || testName === selected.name} onClick={() => void renameSelected()}>Save test name</button></div><p className="form-grid--full">Visual evidence: trace, video, and screenshot are retained for every run for 30 days.</p><div className="form-actions form-grid--full"><button className="button" disabled={!selected}>Create deterministic run</button></div></form>}</>;
}
